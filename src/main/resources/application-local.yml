spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ms-cliente
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: WARN
    org.springframework.web: INFO

api:
  security:
    token:
      expiration-hours: 2
      secret: ${JWT_SECRET:12345678}
